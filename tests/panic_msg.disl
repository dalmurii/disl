; panic_msg with a program's own code (64 and up) exits with 101 too.

routine main() -> I32
    block entry():
        %code: TrapCode = bitcast<I8, TrapCode>(70)
        panic_msg(%code, "configuration is missing")
